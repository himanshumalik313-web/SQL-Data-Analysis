# SQL-Data-Analysis
Analyzed an e-commerce dataset using MySQL by writing SQL queries to extract meaningful business insights through filtering, grouping, aggregation, subqueries, and window functions.
